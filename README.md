# Black_Jack_ML
A model designed to play black jack on a randomised deck. The deck is random in order to prevent simple card counting, forcing the model to learn optimal policies. Download the folder and run the python file within it to view a tk inter ui to mess arounf with different values
